{
  "version": 2,
  "buildCommand": "cd cine && npm install react react-dom react-router-dom @vitejs/plugin-react vite @stripe/react-stripe-js @stripe/stripe-js jspdf jspdf-autotable && npm run build",
  "outputDirectory": "cine/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/cine/dist/$1"
    }
  ]
}